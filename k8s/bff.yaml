apiVersion: apps/v1
kind: Deployment
metadata:
  name: bff
  namespace: ludo
  labels:
    app: bff
spec:
  replicas: 1
  selector:
    matchLabels:
      app: bff
  template:
    metadata:
      labels:
        app: bff
    spec:
      containers:
        - name: bff
          image: ghcr.io/ashwinkrishnaa29/ludo-bff:latest
          ports:
            - containerPort: 4005
          envFrom:
            - configMapRef:
                name: ludo-config
            - secretRef:
                name: ludo-secrets
          readinessProbe:
            httpGet:
              path: /readyz
              port: 4005
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: 4005
            initialDelaySeconds: 10
            periodSeconds: 15
          resources:
            requests:
              cpu: "50m"
              memory: "96Mi"
            limits:
              cpu: "200m"
              memory: "192Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: bff
  namespace: ludo
spec:
  selector:
    app: bff
  ports:
    - port: 4005
      targetPort: 4005
